// Note: In Nuxt 3, components are auto-imported from components/ directory
// This plugin is only needed if you want to register components with different names
// or for backward compatibility

export default defineNuxtPlugin((nuxtApp) => {
  // Components are auto-imported in Nuxt 3
  // No need to manually register them unless you want custom names
  // Example:
  // nuxtApp.vueApp.component('base-modal', BaseModal);
});

